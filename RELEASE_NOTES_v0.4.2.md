# 3458A Multi Lab Studio v0.4.2

本版本修复“第一次打开正常，关闭后再次点击无反应”的启动与退出生命周期问题。

## 修复

- 关闭软件时会主动取消正在阻塞的 VISA I/O，不再只等待 3 秒后留下后台进程。
- 所有采集线程和 GPIB/VISA 会话释放后才完成正常退出。
- 极端情况下，厂商 VISA 原生调用在 20 秒内仍无法取消，软件会记录完整错误并
  结束残留进程，确保 Windows 释放 GPIB 句柄且下次可以重新启动。
- 再次点击程序图标时，如果软件已经运行，会激活现有窗口，避免重复后台实例。
- 保存的窗口坐标位于已断开的显示器或已经损坏时，自动回到主屏幕中央。
- 启动阶段异常会显示明确错误窗口并写入持久日志，不再静默退出。
- EXE 安装到稳定目录
  `%LOCALAPPDATA%\Programs\3458A Multi Lab Studio`，桌面和开始菜单快捷方式不再
  依赖 ZIP 解压目录；移动或删除解压目录后仍可启动。

## 保留

- 中英文即时切换
- A/B/C 任意同步组合
- 固定点数自动停止
- 多通道 FFT/ASD、统计、Allan 与漂移/温漂
- 持久错误日志与诊断报告
