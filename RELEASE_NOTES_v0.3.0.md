# 3458A Multi Lab Studio v0.3.0

## 本次升级

- 顶部可随时切换中文 / English，选择会自动保存。
- A、B 继续默认启用；新增默认关闭的完整 C 通道预留。
- C 启用后拥有独立地址、配置、采集线程、固定点数、读数和分析数据。
- 同步启动自动作用于当前全部启用通道（A+B 或 A+B+C）。
- FFT/ASD、统计分布、Allan、漂移/温漂支持全部启用通道同时分析。
- 通道配色固定：A 青色、B 紫色、C 绿色。
- 多通道 CSV 导出保留每台仪表自己的时间轴，不伪造同步关系。
- 默认 A/B 地址更新为当前硬件结构：
  - A `GPIB0::21::INSTR`
  - B `GPIB1::22::INSTR`
  - C `GPIB2::23::INSTR`（预留）
- 主窗口改为可拖动三栏布局，并保存窗口大小和各栏宽度。
- 静态界面、动态状态、文件操作、分析标题和主要事件均支持双语。

## Windows 启动方式

第一次只运行一次 `INSTALL_ONCE_WINDOWS.bat`。安装脚本会：

1. 建立本地运行环境；
2. 安装依赖；
3. 执行完整测试；
4. 生成 `dist\3458A-Multi-Lab-Studio.exe`；
5. 创建桌面和开始菜单快捷方式；
6. 启动软件。

以后直接使用桌面快捷方式、开始菜单或 EXE，不再运行 BAT。

## 同步边界

软件同步会等待所有启用仪表完成连接和配置，再同时释放采集线程。它适合近同时
启动，但不等于微秒级采样同步。严格同步仍需外部触发硬件。
