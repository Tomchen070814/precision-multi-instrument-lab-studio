# Precision Multi-Instrument Lab Studio v0.5.0

v0.5.0 是一次架构级升级：程序从“最多三台 3458A”扩展为设备无关的多仪器
采集与分析平台，同时保留 v0.4.2 的安全退出、单实例重启、双语、任意同步组合、
固定点数、日志与诊断报告。

## 混合仪器与混合测量

- A/B/C 每个通道可从 10 个厂家、11 款型号中独立选择仪表。
- 3458A 继续使用原生 HP-IB 命令，只接受 GPIB VISA 仪表资源。
- 其余 10 款型号使用配置化 SCPI 适配器，可使用 USB、LAN、串口或 GPIB
  VISA 仪表资源。
- 新增 Keysight 34470A、Fluke 8846A、Keithley DMM7510、R&S HMC8012、
  Rigol DM3068、Siglent SDM3065X、GW Instek GDM-9061、Hioki DM7276、
  Yokogawa DM7560 和 Picotest M3510A 配置文件。
- 每次连接先执行身份查询并核对所选型号；不允许把另一厂家的仪表误当成当前
  型号继续采集。
- 每个型号按能力约束功能列表；例如 Hioki DM7276 是精密 DC 电压表，只开放
  DCV，不虚假显示电阻或电流功能。
- 三个通道可以分别设置 DCV、ACV、二/四线电阻、DCI、ACI、频率或周期。
- 驱动、通道、线程、会话和分析不再以 A/B 或 3458A 型号写死。
- SCPI 型号的厂家专用高速 digitize 暂未冒充实现；v0.5.0 先开放配置化精密
  单次读取，3458A 继续保留独立突发采集。

## 多轴真实时间趋势

- 趋势图使用真实采集时间戳作为 X 轴，不再使用测试经过时间。
- 悬停和固定 Mark 显示通道、仪表型号、数值、单位和本地时间到毫秒。
- Mark 会自动选择鼠标附近最近的 A/B/C 曲线，因此 B/C 均可独立标记。
- V、Ω、A 等不同物理量自动分配独立 Y 轴；同单位通道自动共用一条轴。
- A 固定青色、B 固定紫色、C 固定绿色，坐标轴颜色跟随对应通道。
- 新增“框选放大”：拖动矩形选择时间范围，各单位的 Y 轴按可见数据独立缩放。
- 新增“重置视图”。
- 主监控指标新增最大值和最小值。
- 混合单位时，趋势保持同时显示；FFT/ASD/Allan 跟随所选通道分别分析，避免
  把不同单位错误叠加在同一坐标轴。

## 蓝屏与断联数据保护

- 每个通道启动时自动创建追加式恢复 CSV，不需要用户先选择导出文件。
- 精密采集每收到一个样本，就先写入文件并执行 `flush + fsync`，成功后才加入
  内存会话。
- GPIB/VISA 断联、手动停止、固定点数完成和正常退出都会完成并保留自动保存文件。
- 软件或电脑异常终止时，已经同步到文件系统的 `.partial.csv` 仍可直接打开。
- 下次启动会检测中断文件并在事件区提示，不会覆盖或删除。
- 右侧可直接打开自动保存目录；诊断报告记录保存路径和中断文件数量。
- 高速突发数据仍需先从仪表内存传到电脑，传输完成后整批同步落盘；尚未离开仪表
  的数据无法由电脑软件提前保存。

## 工程质量

- 新增设备型号模型、10 款配置化 SCPI 适配器、单位轴分配逻辑和耐久写盘模块。
- 新增混合 V/Ω/A 三轴、同单位共享轴、真实时间戳、恢复文件、资源过滤和
  34470A SCPI 命令测试。
- 49 项自动测试通过；Ruff 静态检查零问题。
- 当前 Linux 验证环境缺少 `libEGL.so.1`，完整 Qt 窗口测试仍会在 Windows
  一次安装流程中执行。

## Python 技术决策

v0.5.0 不进行没有收益的整套语言重写。当前采集速率主要受仪表积分时间、
GPIB/VISA 和 Windows 调度限制，不受 Python 数值上限限制。设备驱动、采集线程、
持久化与 UI 已解耦；后续若某个高速驱动确实需要原生性能，可以单独用 C++/Rust
实现适配器，而无需重写整个程序。
