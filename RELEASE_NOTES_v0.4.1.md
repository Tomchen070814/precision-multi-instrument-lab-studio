# 3458A Multi Lab Studio v0.4.1

## Windows 一次安装修复

- 修复已有安装记住 English 后，构建测试仍只匹配中文提示而误判失败的问题。
- UI 自动化测试改用独立临时设置文件，不读取、覆盖或清空用户保存的语言、窗口
  布局和 C 通道状态。
- 双通道分析测试改用 pyqtgraph 公共数据接口，兼容空曲线在不同版本中返回
  `None` 或空数组的差异。
- 一次安装过程自动将依赖安装、静态检查、测试和 PyInstaller 输出保存到
  `%LOCALAPPDATA%\3458A Multi Lab Studio\install_logs`；即使 EXE 尚未生成，
  也能保留并发送完整错误日志。
- 保留 v0.4.0 的 A/B/C 任意组合启动、双语界面、多通道分析和持久诊断日志。
- Windows 构建仍会严格执行静态检查和完整测试；测试失败不会生成无效 EXE。

## 本次错误的性质

截图中的两项失败都来自自动化测试断言兼容性，不是 Python、VISA、GPIB 驱动或
真实仪表通信故障。v0.4.1 修正测试隔离与跨版本兼容后再进入 PyInstaller 打包。
