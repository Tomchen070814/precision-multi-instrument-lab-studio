# 3458A Multi Lab Studio v0.4.0

## 任意通道组合启动

- “同步启动”不再写死为全部启用通道。
- 可自由勾选 A+B、A+C、B+C 或 A+B+C。
- 只勾选一个通道时自动变为普通单通道启动。
- 未勾选的运行通道不受影响；资源冲突检查仍覆盖所有实际运行通道。
- C 关闭时不可被选中，避免误启动不存在的第三台仪表。

## 错误留痕与诊断

- 新增应用级 UTF-8 持久化日志，软件关闭或崩溃后仍保留。
- 日志按 5 MB 自动轮转，当前文件加三个备份，限制长期磁盘占用。
- 记录启动/退出、用户事件、VISA 连接、仪表配置、采集线程、CSV 和未捕获异常。
- 右侧新增“导出诊断报告”，一键生成 ZIP。
- 报告包含 HTML 摘要、结构化 JSON、当前事件和历史日志。
- 报告不包含测量样本、CSV 或 Excel 数据。

## UI 与质量

- 视觉系统改为中性深色简约风格，减少高饱和装饰和重复边框。
- A/B/C 同步选择器使用固定通道色和清晰的选中状态。
- 调整字体层级、圆角、间距、状态标签、表单和事件区。
- 中英文同步覆盖新增组合启动和诊断报告功能。
- Windows 构建在测试前增加 Ruff 静态质量检查。
- 通过 29 项非图形自动测试与 Ruff 全量检查；完整 Qt 窗口测试由 Windows
  一次安装脚本在生成 EXE 前再次执行。
